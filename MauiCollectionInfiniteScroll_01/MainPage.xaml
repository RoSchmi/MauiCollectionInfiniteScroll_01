<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pagemodel="clr-namespace:MauiCollectionInfiniteScroll_01"
             xmlns:viewmodels="clr-namespace:MauiCollectionInfiniteScroll_01.ViewModels"
             x:DataType="viewmodels:MainPageViewModel"
             x:Class="MauiCollectionInfiniteScroll_01.MainPage"
             Shell.FlyoutBehavior="Disabled">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <Label
                Margin="0,20,0,0"
                Text="Maui CollectionView Infinite Scroll Example"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />
            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />
            <BoxView BackgroundColor="DarkGray" Margin="0,0,0,5" HeightRequest="20"/>
            <Grid ColumnDefinitions="*,*,*" RowDefinitions="Auto, Auto">
                <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                       Text="Clicking the Go! Button will create a List of Items and will display them in a CollectionView"/>
                <Button  
                    Text="Go !" 
                    Grid.Column="1"
                    Grid.Row="1"
                    Margin="0,40,0,0"
                    BackgroundColor="Green"
                    SemanticProperties.Hint="Counts the number of times you click"
                    TextColor="White" 
                    CornerRadius="12" 
                    HorizontalOptions="Fill" 
                    Command="{Binding GoCommand}"/>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
